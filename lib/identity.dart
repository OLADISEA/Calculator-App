import 'package:flutter/material.dart';
class HomeButton{
  String buttonText;
  Color gridColor;
  Color textColor;
  HomeButton({required this.buttonText, this.gridColor = Colors.grey,this.textColor = Colors.black});
}